{
  "name": "agent-usage-plus",
  "private": true,
  "description": "Dev-only test harness for agent-usage-plus's extracted logic modules. The plugin itself stays Node-free at runtime.",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
